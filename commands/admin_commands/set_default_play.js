module.exports = {
    name: ['setdefaultstatus'],
    description: 'Sets the bot\'s default status',
    use: 'setdefaultstatus [default status]',
    example:['setdefaultstatus LEGO Universe'],
    notes: 'Sets the bot\'s default status',
    async execute(settings, message, args) {

    }
}